$schema: "http://json-schema.org/draft-06/schema"
$id: "https://errorcorrectionzoo.org/schemas/ecc_features"

#
# the features: code object property
#

# type: object

# # todo, we should codify the exact properties and values that can be set under
# # within the `features:` tree.

type: object
additionalProperties: false
required: []
properties:

  rate:
    type: string
    _llm: full

  magic_scaling_exponent:
    type: string
    _llm: full

  encoders:
    type: array
    items:
      type: string
      _llm: full

  general_gates:
    type: array
    items:
      type: string
      _llm: full

  decoders: 
    type: array
    items:
      type: string
      _llm: full

  transversal_gates:
    type: string
    _llm: full

  fault_tolerance:
    type: array
    items:
      type: string
      _llm: full

  threshold: 
    type: array
    items:
      type: string
      _llm: full

  code_capacity_threshold: 
    type: array
    items:
      type: string
      _llm: full


  # We can decide to add new standard feature fields here.
  #
  # ...
